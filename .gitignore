# venv
.venv/
venv/
__pycache__/
*.pyc

# trained artifacts — kept in repo so Streamlit Cloud can serve Layer 1
# (regenerate locally with `python -m src.ercot.train_and_save`)
# data/artifacts/*.pkl

# OS
.DS_Store

# editor
.idea/

# big LBNL file is kept in repo for reproducibility — uncomment to exclude
# data/raw/*.xlsx
